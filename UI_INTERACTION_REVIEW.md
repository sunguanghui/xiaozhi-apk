# 灵犀 Android 客户端 — UI 与交互评审文档

> 本文档详细梳理项目所有 UI 页面、交互流程和用户体验现状，供评审使用。

---

## 一、整体概览

| 属性 | 说明 |
|---|---|
| 项目名称 | 灵犀（Lingxi）Android 客户端 |
| 定位 | 基于小智平台（xiaozhi.me）的 AI 语音助手客户端 |
| 核心功能 | 实时语音对话、文字聊天、官方平台接入、自建服务器支持 |
| UI 框架 | Material Design 3（Material3 组件库） |
| 开发语言 | Java |
| 最低版本 | Android 10（API 29） |

### 应用共有两个页面

1. **MainActivity** — 主界面（语音通话、消息历史）
2. **SettingsActivity** — 设置页面（服务器配置、设备信息）

---

## 二、主界面（MainActivity）

### 2.1 整体布局

```
┌──────────────────────────────────────────┐
│ TopBar: 灵犀      ● 已连接  [⚙]         │  ← AppBar
├──────────────────────────────────────────┤
│                                          │
│        [ 连接 / 断开连接 按钮 ]           │  ← 顶部连接按钮
│                                          │
│   ┌────────────────────────────────┐     │
│   │    通话中…（仅通话时显示）      │     │  ← 通话状态文字
│   │    ~~~~~~~~ 波形动画 ~~~~~~~~  │     │  ← WaveformView
│   └────────────────────────────────┘     │
│                                          │
│         ┌──────────────────┐             │
│         │   大圆形通话按钮  │             │  ← 120dp FAB
│         └──────────────────┘             │
│              点击开始聊天                 │  ← 按钮说明文字
│                                          │
│   ┌────────────────────────────────┐     │
│   │  🚀 操作引导卡片（首次使用）    │     │  ← 首次使用引导
│   └────────────────────────────────┘     │
│                                          │
│   ┌────────────────────────────────┐     │
│   │  💬 聊天记录        [展开 ▼]  │     │  ← 可折叠消息区
│   │  (展开后显示消息列表+文字输入)  │     │
│   └────────────────────────────────┘     │
└──────────────────────────────────────────┘
```

---

### 2.2 TopBar（顶部应用栏）

**组件**：MaterialToolbar  
**背景色**：白色（bg_surface #FFFFFF）

| 元素 | 位置 | 说明 |
|---|---|---|
| 应用名称"灵犀" | 左侧 | 静态文字，不可交互 |
| 状态指示点 | 右侧 | 8dp 圆点，颜色动态变化 |
| 连接状态文字 | 状态点右侧 | 动态文字，与状态点联动 |
| 设置按钮 ⚙ | 最右侧 | 点击跳转设置页 |

**状态点颜色映射**：

| 状态 | 颜色 | 色值 |
|---|---|---|
| 已连接 | 绿色 | #22C55E |
| 未连接 | 灰色 | #9CA3AF |
| 错误 | 红色 | #EF4444 |

**连接状态文字**：
- 未连接时：「未连接」
- 获取激活状态中：「正在获取激活状态…」
- 已连接：「已连接」
- 错误：「错误」

---

### 2.3 连接按钮

**组件**：MaterialButton  
**样式**：圆角（24dp）+ 前缀图标（ic_link）  
**位置**：内容区顶部居中  

| 状态 | 按钮文字 | 点击行为 |
|---|---|---|
| 未连接 | 「连接」 | 官方模式→走 OTA 流程；自建模式→直连 |
| 已连接 | 「断开连接」 | 断开 WebSocket |

---

### 2.4 大圆形通话按钮（核心交互元素）

**组件**：FloatingActionButton（fabCustomSize=120dp）  
**图标尺寸**：56dp  
**阴影**：elevation 6dp  

| 通话状态 | 背景色 | 图标 | 含义 |
|---|---|---|---|
| 待机（未通话）| 亮蓝色 #1D7FFF | ic_call（电话图标）| 点击开始通话 |
| 通话中 | 深蓝色 #1565C0 | ic_call_end（挂断图标）| 点击结束通话 |

**按钮下方说明文字**：「点击开始聊天」（静态文字，不随状态变化）

**点击行为**：
- 未连接时点击 → Toast 提示「请先连接服务器」
- 已连接时点击开始通话 → 按钮变深蓝+挂断图标，显示波形动画
- 通话中点击结束 → 按钮恢复亮蓝+电话图标，隐藏波形动画

⚠️ **问题**：按钮下方「点击开始聊天」文字始终显示，通话中未更新为「点击结束通话」。

---

### 2.5 通话状态区（通话时显示）

通话中可见，通话结束隐藏（`visibility: GONE`）。

| 元素 | 说明 |
|---|---|
| 状态文字（callStatusText） | 显示「通话中…」，字色为 primary 蓝色，18sp |
| WaveformView（波形动画） | 280×120dp，实时反映麦克风音量，自定义 View |

---

### 2.6 操作引导卡片（首次使用）

**触发条件**：SharedPreferences `has_connected` 为 false（即从未连接过服务器）  
**组件**：MaterialCardView，带 primary 色描边  
**关闭方式**：
1. 点击「知道了」按钮手动关闭
2. 首次成功连接服务器后自动关闭

**卡片内容**：

```
🚀 如何开始使用
 ① 点击右上角⚙，配置服务器并保存
 ② 点击蓝色连接按钮，等待状态点变绿「已连接」
 ③ 按下方蓝色大按钮，开始语音聊天！
                              [知道了]
```

---

### 2.7 消息历史区（可折叠）

**组件**：MaterialCardView，内含 RecyclerView + 文字输入区  
**默认状态**：折叠（只显示标题栏）  

**标题栏**（点击可展开/折叠）：
- 左侧：聊天气泡图标 + 「聊天记录」文字
- 右侧：展开箭头图标（展开时旋转 180°，动画时长 200ms）

**展开后显示**：
1. 消息列表（RecyclerView，固定高度 240dp）
2. 文字输入区

**消息气泡样式**：

| 类型 | 对齐 | 背景色 | 字色 |
|---|---|---|---|
| 用户消息 | 右侧 | 蓝色 #1D7FFF | 白色 |
| AI 回复 | 左侧 | 浅灰色 #F3F4F6 | 深色 #111827 |

**文字输入区**：
- 多行输入框（最多 4 行）
- 发送按钮（圆形蓝色背景）
- 仅在 WebSocket 已连接时发送有效；未连接时静默失败

---

## 三、官方平台绑定流程（弹窗交互）

### 3.1 触发条件

用户在设置中开启「接入官方小智平台」，然后点击主界面「连接」按钮。

### 3.2 已绑定设备

OTA 接口返回无 `activation` 字段时：
- 无弹窗，直接建立 WebSocket 连接
- 状态点变绿，连接按钮变为「断开连接」

### 3.3 未绑定设备（验证码弹窗）

OTA 接口返回 `activation` 字段（含验证码）时弹出：

```
┌──────────────────────────────────────┐
│         设备未绑定                    │
│                                      │
│  请在浏览器打开 https://xiaozhi.me   │
│  进入控制台添加设备，输入以下验证码：  │
│                                      │
│           【 123456 】               │
│                                      │
│  绑定完成后对话框将自动关闭…          │
│                                      │
│   [复制验证码]          [放弃绑定]   │
└──────────────────────────────────────┘
```

**关键设计决策**：

| 设计点 | 实现方式 | 原因 |
|---|---|---|
| 不可误触关闭 | `setCancelable(false)` | 防止用户还没看清验证码就关闭 |
| 复制验证码 | 写入系统剪贴板 + Toast 提示 | 方便用户粘贴到浏览器 |
| 复制不关闭对话框 | Positive 按钮点击后不 dismiss | 用户可反复查看验证码 |
| 放弃绑定 | 调用 `stopPolling()` + dismiss | 立即停止后台轮询，节省资源 |
| 自动关闭 | 轮询成功后 dismiss + 连接 | 绑定完成后流畅衔接 |

**后台轮询机制**：
- 每 5 秒请求一次 `/ota/activate` 接口
- 最多轮询 60 次（5 分钟）
- `volatile boolean isPolling` 控制轮询开关
- 超时后 Toast 提示「激活超时，请重新尝试」

---

## 四、设置页面（SettingsActivity）

### 4.1 整体布局

```
┌──────────────────────────────────────┐
│ ← 设置                               │  ← Toolbar（带返回键）
├──────────────────────────────────────┤
│                                      │
│  ┌──── 服务器模式卡片 ─────────────┐  │
│  │ 接入官方小智平台(xiaozhi.me) [●]│  │  ← Switch
│  └────────────────────────────────┘  │
│                                      │
│  ┌──── 官方平台配置卡片 ──────────┐   │  ← 仅官方模式显示
│  │ 设备 ID: xx:xx:xx:xx:xx:xx    │   │
│  └────────────────────────────────┘  │
│                                      │
│  ┌──── 自建服务器配置卡片 ────────┐   │  ← 仅自建模式显示
│  │ WebSocket 地址 [输入框]        │   │
│  │ Token [输入框（脱敏显示）]     │   │
│  │ 启用 Token 认证 [Switch]       │   │
│  └────────────────────────────────┘  │
│                                      │
│           [     保存     ]            │  ← 保存按钮
│                                      │
└──────────────────────────────────────┘
│  ⋮ 右上角菜单：导出调试日志           │  ← overflow menu
```

### 4.2 服务器模式切换

**组件**：SwitchMaterial  
**切换行为**：
- 开启官方模式 → 隐藏自建配置卡片，显示官方配置卡片（动画：`VISIBLE`/`GONE`）
- 关闭官方模式 → 显示自建配置卡片，隐藏官方配置卡片

### 4.3 Token 脱敏显示

- 加载时显示：`前8位明文 + ****`（如 `abc12345****`）
- 获焦时清空，提示「输入新 Token（留空则保持原值不变）」
- 保存时：若字段为空或仍为脱敏格式 → 保留原 Token；否则存储新值

### 4.4 设备 ID

- 来源：Android Wi-Fi MAC 地址（格式：`xx:xx:xx:xx:xx:xx`）
- 只读展示，不可编辑
- 用于 OTA 请求的 `Device-Id` 头

### 4.5 保存行为

- 保存设置 → `SettingsManager.saveSettings()` 写入 SharedPreferences
- 显示 Toast 「设置已保存」
- 自动调用 `finish()` 返回主界面

### 4.6 导出调试日志

- 入口：Toolbar 右侧三点菜单 → 「导出调试日志」
- 行为：读取日志文件，通过 FileProvider + ACTION_SEND 分享
- 失败时 Toast 提示错误信息

---

## 五、交互状态机

### 5.1 连接状态机

```
[未连接]
    │ 点击「连接」（官方模式）
    ▼
[获取激活状态…]
    ├─ 已绑定 ──────────────────────────────→ [建立WebSocket] → [已连接]
    ├─ 未绑定 → [显示验证码弹窗] ──(用户放弃)→ [未连接]
    │               │
    │           (轮询成功)
    │               ▼
    │          [自动关闭弹窗] → [建立WebSocket] → [已连接]
    └─ 网络错误 ──────────────────────────────→ [错误状态]

[已连接]
    │ 点击「断开连接」
    ▼
[未连接]
```

### 5.2 语音通话状态机

```
[待机：蓝色电话按钮]
    │ 点击（已连接）
    ▼
[通话中：深蓝挂断按钮 + 波形动画 + "通话中…"]
    ├─ 点击结束 ──────────────────────────────→ [待机]
    ├─ 断开连接触发 endCall() ────────────────→ [待机]
    └─ AI 说话时（isPlaying=true）→ 麦克风暂停，等待 AudioTrack 排空 → 麦克风恢复
```

---

## 六、问题清单与优化建议

### P0（影响核心功能）

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 1 | 未连接时点击通话按钮仅弹 Toast，没有引导用户去连接 | MainActivity | 改为高亮连接按钮或弹窗引导 |

### P1（影响用户体验）

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 2 | 通话按钮下方「点击开始聊天」文字通话中不更新 | activity_main.xml | 通话中改为「点击结束通话」 |
| 3 | 消息历史默认折叠，用户可能不知道有消息区 | activity_main.xml | 考虑首次收到消息时自动展开 |
| 4 | 文字发送按钮未连接时静默失败 | MainActivity | 应有 Toast 或禁用状态 |
| 5 | OTA 超时后状态点变黄「错误」，没有明确的重试入口 | MainActivity | 超时后提供「重新连接」按钮或 Toast 带操作 |

### P2（细节优化）

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 6 | 连接过程中连接按钮未禁用，可重复点击 | MainActivity | OTA 请求期间禁用按钮 |
| 7 | 设置页 Token 输入框脱敏逻辑以「****」结尾判断，不够健壮 | SettingsActivity | 增加 `actualToken` 未修改的额外判断 |
| 8 | 波形动画仅反映麦克风输入，AI 说话时波形静止 | WaveformView | AI 说话时可显示输出音量波形 |
| 9 | 消息历史 RecyclerView 固定高度 240dp，内容少时留有空白 | activity_main.xml | 改为 `wrap_content` + `maxHeight` 限制 |
| 10 | 引导卡片「知道了」按钮文字较小（14sp），点击区域偏小 | activity_main.xml | 增大点击区域至 48dp 高 |

---

## 七、颜色与视觉规范

### 主色系

| Token | 色值 | 用途 |
|---|---|---|
| primary | #1D7FFF | 连接按钮、通话按钮待机态、消息气泡（用户）、波形色 |
| primary_dark | #1565C0 | 通话按钮通话中状态 |
| accent | #40C4FF | 波形颜色 |

### 状态色

| Token | 色值 | 用途 |
|---|---|---|
| status_connected | #22C55E | 状态点（已连接） |
| status_disconnected | #9CA3AF | 状态点（未连接） |
| status_error | #EF4444 | 状态点（错误） |

### 背景色

| Token | 色值 | 用途 |
|---|---|---|
| bg_page | #F5F7FA | 页面底色 |
| bg_surface | #FFFFFF | AppBar、卡片 |
| bg_input | #F3F4F6 | 文字输入区背景 |

---

## 八、关键 UI 组件清单

| 组件 | 使用位置 | 说明 |
|---|---|---|
| MaterialToolbar | 两个页面 | AppBar，设置页带返回键 |
| FloatingActionButton | 主界面 | 120dp 大圆形通话按钮 |
| MaterialButton | 主界面、设置页 | 连接/保存按钮，圆角 24dp |
| MaterialCardView | 主界面、设置页 | 引导卡片、消息区、配置区 |
| SwitchMaterial | 设置页 | 官方模式/Token 开关 |
| RecyclerView | 主界面 | 消息历史列表 |
| TextInputEditText | 设置页 | 配置输入框 |
| WaveformView | 主界面 | 自定义波形动画 View |
| MaterialAlertDialogBuilder | 主界面 | 绑定验证码弹窗、bind 消息弹窗 |

---

## 九、无障碍与可用性

| 项目 | 现状 | 评估 |
|---|---|---|
| contentDescription | 通话按钮、设置按钮均有设置 | ✅ |
| 触摸目标尺寸 | 通话按钮 120dp，足够大 | ✅ |
| 颜色对比度 | 主色蓝底白字，对比度约 4.8:1，满足 AA | ✅ |
| 深色模式 | values-night/colors.xml 存在，有基础支持 | ⚠️ 未全面验证 |
| 字体缩放 | 使用 sp 单位，支持系统字体缩放 | ✅ |
| 引导卡片「知道了」按钮 | 高度 36dp，低于 48dp 最佳实践 | ⚠️ 偏小 |

---

## 十、待评审重点建议

请重点关注以下几个方面：

1. **连接流程的完整性**：从点击「连接」到可以开始语音对话，中间经历多个异步步骤，用户反馈是否足够清晰？
2. **绑定弹窗设计**：`setCancelable(false)` 是否过于强制？「放弃绑定」后没有重新发起的快捷入口。
3. **通话按钮标签文字**：「点击开始聊天」在通话中状态未更新，是否会造成用户困惑？
4. **消息历史区默认折叠**：是否会导致用户忽视 AI 回复内容？
5. **错误状态的恢复路径**：无论是 OTA 超时还是 WebSocket 断开，用户恢复路径是否足够清晰？
